Harika, o zaman **PipelineLens v3 (Guided Mode)** için tüm yapılacakların kapsamını net bir şekilde listeleyelim.
Aşağıda hem **şu an tamamlananlar**, hem de **eklenmesi planlanan/iyileştirme gerektiren** tüm maddeleri gruplandırılmış şekilde görebilirsin 👇

---

## ✅ **1. Mevcut Tamamlanan Özellikler (v3 build)**

Bunlar v3 HTML dosyasında **aktif durumda** ve çalışıyor:

### 🧩 Temel Görsel Yapı

* [x] Modern responsive arayüz (dark/light tema)
* [x] Pipeline aşama çubuğu (FlowBar: Rename → Dispatch → Issue → Execute → WB → Commit)
* [x] Cycle kontrol butonları (Step, Auto, Prev, Reset)
* [x] RAT, ROB, IQ, PRF, Functional Units, Timeline kartları
* [x] Mini terminal (Guided Terminal) + verbose seçeneği
* [x] PRF paneli açılır/kapanır
* [x] PipelineLens Panel (instruction aşamaları özet penceresi)
* [x] Tema değiştirici (🌙 / ☀️)
* [x] Cycle başına popup ("Cycle Info Toast")

---

## 🚀 **2. Guided Mode (Öğretici Mod) Özellikleri**

Bu kısım, interaktif ve öğretici akış için planlandı.
Bazı temel kısımları hazır (örneğin Rename Step), bazıları geliştirilecek:

### ✴️ Rename Step Mode

* [x] "Run Rename Step" butonu ile RAT & PRF adım adım güncelleniyor
* [x] Her rename sonrası terminal ve popup bilgi veriyor
* [ ] Henüz **Dispatch aşamasına** geçmiyor (ilerletilebilir)
* [ ] Rename yapılan register’lar animasyonla vurgulanmalı (highlight geçici renk değişimi)

### ⚙️ Guided Terminal

* [x] Cycle bazlı event log yazılıyor
* [x] Verbose seçeneği aktif
* [ ] “Pedagojik açıklama modu”: her önemli olay için açıklama (ör. *"ROB head commit: architectural state update complete"*)
* [ ] “Cycle summary” kutucuğu (ör. *"Cycle 6 — 2 instruction issued, 1 committed"*)

### 🧠 Guided Flowbar (Eğitimsel Vurgu)

* [x] `setFlowActive()` her aşamada highlight yapıyor
* [ ] Bu highlight’a kısa açıklama eklenmeli (tooltip veya info box)
* [ ] “Stage-by-stage guide” modu eklenmeli → kullanıcı sırayla Rename→Dispatch→Issue→Execute→Commit aşamalarını adım adım yürütür.

---

## 📊 **3. Performans & Analiz Kartları**

* [x] IPC, Active ROB, ALU/MDU kullanım istatistikleri hesaplanıyor
* [x] Her 10 cycle’da mini insight popup (ör. *"IPC=0.83 | MDU latency limits throughput"*)
* [ ] Daha kapsamlı “Performance Insight” kartı eklenmeli:

  * Peak IPC
  * Average latency per FU
  * Issue rate histogram (ileride küçük grafik olarak)

---

## 🧱 **4. Görsel İyileştirmeler**

* [x] Cycle toast (fade-in/out)
* [ ] Toast içinde renk kodu (Issue→sarı, Execute→mavi, WB→yeşil, Commit→mor)
* [ ] PRF ve RAT’ta highlight geçişleri (renkli animasyon)
* [ ] Timeline’da mouse hover ile instruction detay gösterimi

---

## 🧰 **5. Teknik İyileştirmeler**

* [ ] State kaydını localStorage’a (kaldığı yerden devam)
* [ ] Kod modülerleştirilecek (JS fonksiyonları gruplara ayrılacak: core, render, ui)
* [ ] Issue Queue ve FU’larda sıradaki adımların görseli eklenecek (ör. pipeline bubbles)
* [ ] Reset sonrası FlowBar highlight “Rename”da sabitlenecek (şu an bazen kalıyor)

---

## 🧩 **6. (Opsiyonel) Genişleme Fikirleri**

İlerleyen sürümler için ek fikirler:

| Alan                       | Öneri                                                                                 |
| -------------------------- | ------------------------------------------------------------------------------------- |
| 🔍 Instruction Tracker     | Belirli instruction (#id) seçilince onun tüm aşamalarını timeline üzerinde renklendir |
| 🎥 Replay Mode             | Tüm simülasyonu animasyonlu olarak baştan sona göster                                 |
| 📘 Eğitim Modu             | “Next lesson → Rename”, “Next → Issue” butonlarıyla rehberli ilerleme                 |
| ⚙️ Configurable Parameters | ALU/MDU latency, issue width, commit width kullanıcı tarafından değiştirilebilir      |
| 📈 Graph Mode              | Cycle-by-cycle IPC ve resource utilization grafiği (mini chart)                       |

---

## 🧭 **Sonraki Adım (v3.1 için öneri plan)**

Sırasıyla şu adımlar mantıklı olur:

1. 🔹 Rename highlight animasyonu ekle
2. 🔹 Cycle info popup renklerini olay türüne göre ayır
3. 🔹 Guided Terminal’e pedagojik açıklamalar ekle
4. 🔹 FlowBar’a stage açıklamaları ekle (tooltip veya mini info box)
5. 🔹 Performans insight kartını GUI içinde sabit bölüme taşı

---

İstersen ben şimdi v3.1’de bu listedeki **ilk üç maddeyi (Rename highlight + toast renkleri + pedagojik terminal)** ekleyebilirim.
Hazır mısın bu güncellemeyi başlatayım mı?
